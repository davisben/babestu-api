table! {
    users (id) {
        id -> Integer,
        email -> Text,
        password -> Text,
        first_name -> Text,
        last_name -> Text,
        created -> Timestamp,
        modified -> Timestamp,
    }
}
